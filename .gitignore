# 依赖
node_modules/

# 52MB 的 cloudflared 二进制。
#
# 为什么不提交：代码**不读仓库里的这份**。cloudflared 的查找顺序是
#   配置里指定的 → 系统 PATH → dsh-pocket 下过的那份 → 自己下载到数据目录
# 仓库里的 bin/ 不在其中；package.json 的 files 也只打包 lib/client/cordis.patch.yml/README.md。
# 提交它等于凭空给仓库加 52MB，而且 GitHub 对超过 50MB 的单文件会告警。
# 首次开公网访问时插件会自己下载（带国内加速源），README 里也是这么跟用户说的。
bin/

# 立绘生成过程的原图，49 个文件约 50MB。
# 成品是 lib/art/*.webp（已提交）。这些是中间产物，不进仓库。
# ⚠️ 但它们是**不可再生的**：绘图模型每次出的图都不一样，删了就得重画。
#    所以不进 git 不等于可以删，建议另找地方备份一份。
dsh-image-gen/

# 内部文档，不进仓库（2026-09-22 用户裁决）
#
# 对外发布的只有 README.md。这三样是给我们自己看的：工程说明是设计与取舍的
# 完整记录，使用指南的内容已并入 README，tasks/ 是开发过程的工作记忆。
#
# ⚠️ 代价要说清楚：它们因此**没有版本保护**了。2026-09-22 就出过一次事——
#    tasks/lessons.md 被误覆盖，1363 行变 79 行，因为没有 git，无处找回。
#    要同时做到「不公开」和「有版本保护」，办法是另建一个**私有**仓库装它们
#    （或者用 git 的 --git-dir 指到仓库外面）。目前没做，属于已知的取舍。
使用指南.md
工程说明.md
tasks/

# 编辑器与系统垃圾
.DS_Store
Thumbs.db
*.log
.tmp/
