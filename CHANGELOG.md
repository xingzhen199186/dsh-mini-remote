# 更新日志

本文件记录每个版本改了什么。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 新加的

- **手机上也能用 `/` 指令了。** 在输入框里打一个 `/`，输入框上方就会列出这个会话能用的指令（名字 + 一句说明），继续打字按名字过滤，点一下就执行；需要参数的指令会把它填进输入框、并把「它要什么」挂在提示位置，你补完再发。指令跑出来的结果在聊天里单独一行，和 AI 说的话、你自己说的话都长得不一样。**这份名单是 DSH 自己的账本**，不是插件的——电脑上新装一个带指令的插件，手机上刷新就有了。本机这台电脑当前有 6 条：`compact`、`export`、`feedback`、`goal`、`permission`、`plan`。
  - 打错的指令、不存在的指令：**拒绝并说明**（和电脑端同一条规则），不会退回去当成一次提问发给模型。`/model`、`/file` 会特别说一句「这是电脑网页端专有的」——它们确实只活在浏览器那半边，宿主账本里没有。
  - 会话没在跑（睡在硬盘上）时没有指令可列：这时如实说「这个会话现在没在跑，指令要先让它跑起来」，**不显示成一个空名单**（那等于说「一条指令都没有」，是另一回事）。
  - 指令执行会写进会话记录（`command/run` + `command/done` 两条日志事件），所以点开一个老会话时，之前执行过的指令和结果也在——手机上看到的和电脑上是同一份账。
  - **指令这一版带不了附件**：DSH 的指令要的附件凭据和手机上传那套不通。输入框里贴着的附件会**原样留着**（不会被悄悄丢掉），跟下一条普通消息一起发出去。

### 修好的

- **指令跑完时手机上可能一直挂着「执行中…」（最多一分钟）。** 起因是手机一分钟会重读一次会话日志，如果读的那一刻指令还在跑，记录里那条就写着「执行中」；而它和内存里那条（已经拿到结果）时间戳相同，按原来的合成规则接不上去，于是内存里那份更准的反而没显示出来。现在指令行按 `commandId` 认人，记录里那条就地把结果换成内存这份的。配了一条反向验证（把修复去掉，那条用例确实变红）。

## [0.1.2] - 2026-09-25

### 新加的

- **手机点开一个会话，就能看到它之前每一轮。** 在这之前，手机上只有「插件亲眼看过」的内容：内存里那份历史从 DSH 启动、或者从手机开始遥控的那一刻才开始记，所以一个早就存在的会话点进去是空的。现在点开会话（以及在手机上刷新页面、切换会话）时，插件会去读那个会话的记录，把每一轮的用户指令和最终结论列出来；单帧模式仍然只显示最后一条结论。读不出来（老格式日志、写了一半的日志）就退回手里那份并记一条日志，**不编内容**。
  - 重放和实时走的是**同一套判断**：`lib/history.js` 调的就是 `events.js` 里那个提取器（`createTurnTracker`），两条路的「哪条才算回答」不会走散。
  - 读完就广播一份新快照。手机上看到的顺序是「先有手里那份，再自然补全」，不会空白等待。
  - 顺手修掉了 `tools/peek-events.mjs` 的两个真 bug：多帧 zstd 日志只解第一帧（26MB 的日志只读出 196 字节，看着像「这个会话没内容」）、文件名写死 `session.v3.jsonl.zstd`（v4 的会话根本找不到）。它现在直接用 `lib/log-tail.js` 的帧扫描和文件查找。

### 修好的

- **开机预热历史撞上「巨型父会话」会把 DSH 本体 OOM 打挂。** 绑定会话如果是**带大量子会话的父会话**（本机实测：310 个子会话、26MB 压缩日志），`sessionQuery.readSession()` 会触发 v3→v4 迁移并把这个父会话的**每个子会话整份解码**，默认约 4GB 堆 20 秒就没了。现在预热前先量规模（`lib/tree.js` 的 `sessionScale`：用已缓存的 `listSessions()` 记录数子会话 + 只 stat 自己的日志体积），超过 `HISTORY_MAX_CHILDREN = 20` 或 `HISTORY_MAX_BYTES = 8MB` 就**不走整份读，改走下面那条有上限的尾部读**（日志里记一条「规模上限：… 改走尾部读」，带上子会话数与日志体积）；`pending` 标记仍是**同步**打上，手机侧「正在读」的语义不变。
  - 为什么非要挡在调用之前：`readSession` 一旦被调用，代价发生在 DSH 内核里（迁移 → 全解子会话 + 两次全量克隆），事后省不回来。
  - 实测：绑回那个 310 子会话的父会话，堆峰 521MB（改之前 4364MB → OOM）；`npm test` 458/458。

### 改了的

- **手机端读会话历史有了上限（用户 2026-09-25 的要求：读历史要设上限）。** 三层都是常量、都跟会话总量无关：
  ① **读取窗口** `lib/log-tail.js`（新）——只解日志**尾部**若干 zstd 帧：`HISTORY_TAIL_WINDOW_BYTES = 16MB`、`HISTORY_TAIL_MAX_EVENTS = 20000`；窗里一个完整帧都没有（最后一帧比窗口大）时会**放大窗口重来**（4×，上限 64MB），不是直接认输。
  ② **展示条数** `HISTORY_MAX_ENTRIES = 200`——重放成聊天记录后只留尾部这么多。
  ③ **诚实标注**：`store.snapshot()` 多出 `historyTruncated` / `historyNote`，手机端在聊天内容**最上面**垫一行「这个会话很大，只显示了最近 N 条（读取上限：…）」——条数被砍了却不说，用户会以为这个会话就这么点。
  - 判定：会话「大」（子会话 > 20 或日志 > 8MB）走有上限的尾部读；「小」仍走内核的完整读（日志权威、会话活着还免读盘）。窗口把整份都盖住时**不谎报**截断。
  - 实测（本机最坏那条 26MB / 2.2 万事件的父会话）：4MB 窗口→29 条 / 121ms；8MB→87 条 / 203ms；16MB→197 条 / 472ms；32MB（等于整份）→251 条 / 764ms / 堆峰约 540MB。同一份日志走内核 `readSession` 是 4GB 堆 OOM，所以尾部读的代价小两个数量级。
  - 验收：把绑定会话设成那个巨型父会话，`dsh web` 起来堆峰 477MB（不再 OOM），手机 `/mini/api/state` 拿到 **197 条真实历史 + `historyTruncated: true` + 说明文案**；`npm test` **458/458**（新增 `test/log-tail.test.mjs` 8 条、`test/page.test.mjs` 2 条）。

## [0.1.1] - 2026-09-25

### 新加的

- **手机上能回答 AI 的提问了。** 电脑那边的 AI 问你「选哪个」（比如让你挑一个方案）时，问题会推到手机上，点一下就算答了。一个一个答，答错了能回头改，选项里没有的也能自己打字。手机上要是没人（锁屏、关了页面、断了网），问题会自动还给电脑，那边照常弹窗——不会两头都答不上。
- **权限档位能在手机上切。** 仅可查看 / 工作区内修改 / 完全权限，不用再回电脑改，档位显示中文名。
- **每个会话的模型和思考强度能在手机上换。** 顶栏那一行就是当前用的模型，点它就能改。会话没在跑（睡在硬盘上）的时候也读得出它上次用的是什么，不用先跑一轮。
- **手机上能新建工作区和会话。** 浏览电脑上的目录，挑一个登记成工作区，再在里面开会话。
- **Tailscale 那条路上多了一个加密地址**（形如 `https://你的电脑名.你的网络名.ts.net/`）。它和原来那条是同一台电脑、同一条通道，区别只在连接加不加密。加密之后浏览器才肯给用系统通知和麦克风——要那两样，前提是「加密连接」。

### 修好的

- Tailscale 那条路：找不到命令时会把该装什么说清楚，检查状态不再让用户干等十五秒；加密地址那条的存在和前提也补上了说明。
- 开启通知的引导：HarmonyOS 上不再让用户去点一个不存在的东西；权限被拒过之后，会告诉用户「清数据」这条路。
- 「单帧」和「聊天」两种模式里，新回答落下来时的滚动位置都不再停在末尾。
- 文案：「锁屏也能提醒」改成「页面在后台时提醒」——它做不到锁屏提醒，原来那句话承诺过头了。

### 已知的取舍

- 只有 Tailscale 的加密地址和公网隧道是 `https://`。明文那条路上，浏览器不给用系统通知和麦克风。
- 手机上答的是 AI 主动提给你的选择题。**系统层面的确认弹窗（比如危险命令的审批）手机上点不了**，还得回电脑。
- 公网隧道的地址每次重启都会换。

## [0.1.0] - 2026-09-22

第一个版本。

### 有这些

- 手机上只收到两样东西：你发出去的指令，和 AI 的最终结论。工具调用、文件读写、子 Agent 调度、思考过程全部留在电脑上。
- 三条连接路径并存，由你自己选：同一局域网、Tailscale、Cloudflare 公网隧道。
- DSH 设置页里的「手机遥控」标签：每条路各一条链接、一个二维码、一个密码。二维码里编了访问口令，手机扫码即用，不用手输。
- 公网隧道地址会自己盯着。cloudflared 打印出网址不等于隧道能用——插件会真的往那个地址发一次请求，确认通了才把地址给你；断了会把地址撤下来并说明原因。
- 电脑端的外部推送（Bark / ntfy / Telegram），人不在电脑前也能收到提醒。

### 已知的取舍

- 三条路都是明文 http，所以手机浏览器不给用麦克风、也不许发系统通知。界面上没有这两项，是有意为之。
- 公网隧道的地址每次重启都会换，需要重新扫码。想要固定地址就得自己配一条命名隧道（要域名 + Cloudflare 账号），这一版没做。
- 公网隧道容易被本机的代理或 VPN 干扰。遇到连不上，设置页里会直接告诉你去关代理，或者改走 Tailscale。

### 没有这些

- 不做桌面镜像。手机上看不到会话列表、文件树、工具调用详情——这是设计目标，不是还没做。

[0.1.0]: https://github.com/xingzhen199186/dsh-mini-remote/releases/tag/v0.1.0
[0.1.1]: https://github.com/xingzhen199186/dsh-mini-remote/releases/tag/v0.1.1
[0.1.2]: https://github.com/xingzhen199186/dsh-mini-remote/releases/tag/v0.1.2
